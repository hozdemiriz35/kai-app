[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    Permissions-Policy = "camera=*, microphone=()"
    X-Frame-Options = "SAMEORIGIN"
    X-Netlify-Original-Pathname = "no-inject"
